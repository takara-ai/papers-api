{
  "builds": [
    { "src": "api/*.go", "use": "@vercel/go" }
  ],
  "routes": [
    { "src": "/api$", "dest": "/api" },
    { "src": "/api/(.*)", "dest": "/api" }
  ],
  "crons": [
    {
      "path": "/api/update",
      "schedule": "0 0 * * *"
    }
  ]
} 